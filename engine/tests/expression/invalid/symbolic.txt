# Unknown identifiers still error — only the curated single-letter symbol set
# (x, y, z) is recognised. Glued multi-letter runs are NOT split into a product.
xy
xyz
apples
2 apples
diff(x^2, a)
limit(x, a, 0)
solve(x^2, x)
x to meter
